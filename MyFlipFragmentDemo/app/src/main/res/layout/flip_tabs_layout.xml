<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >    

   <zxyilian.myflipfragmentdemo.flip.TitleIndicator
        android:id="@+id/pagerindicator"
        android:layout_width="fill_parent"
        android:layout_height="40dp"
        android:background="@android:color/white"
        app:footerColor="@color/app_blue"
        app:footerLineHeight="2.5dp"
        app:footerTriangleHeight="3.0dp"
        app:textColor="@color/text_black"
       android:paddingTop="1dp"
        app:textSizeNormal="@dimen/page_tab_normaltitlesize"
        app:textSizeSelected="@dimen/page_tab_selecttitlesize" /> 

    <!-- Don't set background for ViewPager. It doesn't work because of page margin -->

    <zxyilian.myflipfragmentdemo.flip.ViewPagerCompat
        android:id="@+id/pager"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:duplicateParentState="true" />

</LinearLayout>