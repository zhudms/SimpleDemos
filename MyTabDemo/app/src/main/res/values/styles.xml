<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="tab_listmenu_title" parent="menu_parent">
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">middle </item>
        <item name="android:textSize">36sp</item>
        <item name="android:minHeight">@dimen/menu_item_titleheigh</item>
        <item name="android:paddingLeft">@dimen/menu_item_titlepaddingleft</item>
        <item name="android:paddingTop">@dimen/menu_item_titlepaddingbottom</item>
        <item name="android:paddingBottom">@dimen/menu_item_titlepaddingbottom</item>
    </style>

    <style name="menu_parent" parent="wrap">
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:gravity">center_vertical</item>
    </style>
    <style name="wrap">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#4433ee</item>
    </style>
</resources>
